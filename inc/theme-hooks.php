<?php
/**
 * Circle hooks.
 *
 * @package   elementor circle
 * 
 * @since 		 1.0.0
 */


if ( ! defined( 'ABSPATH' ) ) {
	exit( 'Direct script access denied.' );
}

/**
 * Head top
 */
function circle_topbar_before() {
	do_action( 'circle_topbar_before' );
}

/**
 * Head top
 */
function circle_topbar_after() {
	do_action( 'circle_topbar_after' );
}

/**
 * Head top
 */
function circle_head_top() {
	do_action( 'circle_head_top' );
}

/**
 * Head bottom
 */
function circle_head_bottom() {
	do_action( 'circle_head_bottom' );
}

/**
 * Header before
 */
function circle_header_before() {
	do_action( 'circle_header_before' );
}

/**
 * Site Header
 */
function circle_header() {
	do_action( 'circle_header' );
}

/**
 * Header after
 */
function circle_header_after() {
	do_action( 'circle_header_after' );
}

/**
 * Page header before
 */
function circle_page_header_before() {
	do_action( 'circle_page_header_before' );
}

/**
 * Page header after
 */
function circle_page_header_after() {
	do_action( 'circle_page_header_after' );
}

/**
 * Masthead
 */
function circle_masthead() {
	do_action( 'circle_masthead' );
}

/**
 * Menu before
 */
function circle_menu_before() {
	do_action( 'circle_menu_before' );
}

/**
 * Menu after
 */
function circle_menu_after() {
	do_action( 'circle_menu_after' );
}

/**
 * Last menu item after
 */
function circle_last_menu_item_after() {
	do_action( 'circle_last_menu_item_after' );
}

/**
 * Body top
 */
function circle_body_top() {
	do_action( 'circle_body_top' );
}

/**
 * Body bottom
 */
function circle_body_bottom() {
	do_action( 'circle_body_bottom' );
}

/**
 * Primary content top
 */
function circle_primary_content_top() {
	do_action( 'circle_primary_content_top' );
}

/**
 * Primary content before
 */
function circle_primary_content_before() {
	do_action( 'circle_primary_content_before' );
}

/**
 * Primary content query
 */
function circle_primary_content_query() {
	do_action( 'circle_primary_content_query' );
}

/**
 * Primary content after
 */
function circle_primary_content_after() {
	do_action( 'circle_primary_content_after' );
}

/**
 * Primary content bottom
 */
function circle_primary_content_bottom() {
	do_action( 'circle_primary_content_bottom' );
}

/**
 * Comments before
 */
function circle_comments_before() {
	do_action( 'circle_comments_before' );
}

/**
 * Comments
 */
function circle_comments() {
	do_action( 'circle_comments' );
}

/**
 * Comments after
 */
function circle_comments_after() {
	do_action( 'circle_comments_after' );
}

/**
 * Entry section before
 */
function circle_entry_section_before() {
	do_action( 'circle_entry_section_before' );
}

/**
 * Content before
 */
function circle_content_before() {
	do_action( 'circle_content_before' );
}

/**
 * Content top
 */
function circle_content_top() {
	do_action( 'circle_content_top' );
}

/**
 * Content bottom
 */
function circle_content_bottom() {
	do_action( 'circle_content_bottom' );
}

/**
 * Content after
 */
function circle_content_after() {
	do_action( 'circle_content_after' );
}

/**
 * Entry featured image before
 */
function circle_entry_featured_image_before() {
	do_action( 'circle_entry_featured_image_before' );
}

/**
 * Entry featured image
 */
function circle_entry_featured_image() {
	do_action( 'circle_entry_featured_image' );
}

/**
 * Entry featured image after
 */
function circle_entry_featured_image_after() {
	do_action( 'circle_entry_featured_image_after' );
}

/**
 * Entry content top
 */
function circle_entry_content_top() {
	do_action( 'circle_entry_content_top' );
}

/**
 * Entry content bottom
 */
function circle_entry_content_bottom() {
	do_action( 'circle_entry_content_bottom' );
}

/**
 * Page title before
 */
function circle_page_title_before() {
	do_action( 'circle_page_title_before' );
}

/**
 * Page title after
 */
function circle_page_title_after() {
	do_action( 'circle_page_title_after' );
}

/**
 * Sidebar before
 */
function circle_sidebar_before() {
	do_action( 'circle_sidebar_before' );
}

/**
 * Sidebar after
 */
function circle_sidebar_after() {
	do_action( 'circle_sidebar_after' );
}

/**
 * Footer before
 */
function circle_footer_before() {
	do_action( 'circle_footer_before' );
}

/**
 * Footer
 */
function circle_footer() {
	do_action( 'circle_footer' );
}

/**
 * Footer after
 */
function circle_footer_after() {
	do_action( 'circle_footer_after' );
}
